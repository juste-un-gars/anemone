# Anemone - Environment Configuration Example
# Copy this file to .env and adjust values as needed
# DO NOT commit .env to version control!

# === DIRECTORIES ===
# Main data directory (contains db/, certs/, smb/)
ANEMONE_DATA_DIR=/srv/anemone

# User shares directory (can be on separate disk, e.g., ZFS pool)
# Default: $ANEMONE_DATA_DIR/shares
#ANEMONE_SHARES_DIR=/srv/anemone/shares

# Incoming backups from peers (can be on separate disk, doesn't need ZFS redundancy)
# Default: $ANEMONE_DATA_DIR/backups/incoming
#ANEMONE_INCOMING_DIR=/srv/anemone/backups/incoming

# === NETWORK ===
# HTTP port (only used if ENABLE_HTTP=true)
PORT=8080

# HTTPS port
HTTPS_PORT=8443

# Enable HTTPS (default: true, recommended for production)
ENABLE_HTTPS=true

# Enable HTTP (default: false, not recommended for production)
ENABLE_HTTP=false

# === TLS CERTIFICATES ===
# Custom TLS certificate paths (optional)
# Default: $ANEMONE_DATA_DIR/certs/server.crt and server.key
#TLS_CERT_PATH=/path/to/custom/cert.crt
#TLS_KEY_PATH=/path/to/custom/key.key

# === LOGGING ===
# Log level: debug, info, warn, error (default: uses DB setting or "warn")
ANEMONE_LOG_LEVEL=info

# Log directory (default: $ANEMONE_DATA_DIR/logs)
#ANEMONE_LOG_DIR=/srv/anemone/logs

# === ONLYOFFICE INTEGRATION ===
# Enable OnlyOffice document editing (requires Docker)
#ANEMONE_OO_ENABLED=false

# Internal URL of OnlyOffice Document Server container
#ANEMONE_OO_URL=http://localhost:9980

# JWT shared secret for secure communication with OnlyOffice
# Generate with: openssl rand -hex 32
#ANEMONE_OO_SECRET=

# === LOCALIZATION ===
# Language: fr, en (default: fr)
LANGUAGE=fr
